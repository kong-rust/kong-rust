---
status: ACTIVE
---
# CEO Plan: AI Gateway 四子网关战略定位

Generated by /plan-ceo-review on 2026-03-21
Branch: dawxy/docs-cleanup-readme | Mode: SELECTIVE EXPANSION
Repo: kong-rust

## Vision

### 10x Check

统一 AI 网关控制平面 — 不只是代理流量，而是编排 AI 工作流。开发者在一个控制台里看到：所有 LLM 调用的成本仪表盘、Agent 间的通信拓扑图、MCP 工具调用链路追踪。这不只是网关，是 AI 基础设施的可观测性中心。

> **注意：** 控制台 UI 不在当前路线图范围内。Phase 0-4 聚焦网关引擎和 Admin API，可观测性数据通过 Prometheus 指标 + Admin API 暴露。控制台 UI 作为未来方向（可基于 Kong Manager 扩展）。

### 核心战略变更

1. **产品定位重塑**：从"传统 API 网关 + AI 网关引擎"二分法 → "AI 网关"上位概念，下辖 API/LLM/Agent/MCP 四个子网关
2. **执行路径变更**：从线性 Phase 0→1→2→3→4 → 双轨并行 Phase 0+2a 同时启动
3. **Phase 2 拆分**：原始 6 个能力（2a Token 限流、2b 多模型 LB+Fallback、2c Virtual API Key、2d Token 成本追踪、2e 语义缓存、2f Prompt Guard）从一次性交付 → 重组为 4 个增量交付单元

## Scope Decisions

| # | Proposal | Effort | Decision | Reasoning |
|---|----------|--------|----------|-----------|
| 1 | 通用限流器基础设施 | M→S | ACCEPTED | LLM/MCP/Agent 三个子网关都需要限流，一次设计三次复用 |
| 2 | Phase 2 拆分为可交付单元 | S→S | ACCEPTED | 最快有 AI 代码可演示，验证 kong-ai crate 架构 |
| 3 | Agent Gateway 定义补全 | M→S | ACCEPTED | 四子网关中最模糊的一个，需要对标产品和边界定义 |
| 4 | 竞品表 + 架构图更新 | S→S | ACCEPTED | 文档与产品定位一致性 |

## Accepted Scope (added to this plan)

- 通用限流器设计为可复用基础设施（kong-ai crate 内）
- Phase 2 拆分：2a-MVP → 2a-Full → 2b → 2c
- Agent Gateway 补充对标产品、能力清单、与 MCP 边界
- 竞品表增加 Portkey/Helicone/Cloudflare AI GW
- 架构图增加 kong-ai/kong-mcp/kong-agent crate

## Deferred to TODOS.md

- Phase 3/4 详细任务拆分（待 Phase 2 架构验证后细化）

## 执行假设

- **人力模型**：CC+gstack 驱动开发，单人+AI 可支撑双轨并行。轨道 A 和轨道 B 交替推进，非同时编码。
- **Effort 标注说明**：S=几小时, M=1-3天, L=1-2周, XL=2-4周（均为 CC+gstack 时间）。表格中 `M→S` 表示 human 时间→CC 时间。

## 更新后的路线图

### 执行路径（双轨并行）

```
时间线 ─────────────────────────────────────────────►

轨道 A（传统网关加固）:
  Phase 0 ──────► Phase 1a ──► Phase 1b ──► Phase 1c
  (spec 对齐)     (V1 全量)   (V2 增量)   (运维增强)

轨道 B（AI 网关）:
  Phase 2a-MVP ──► Phase 2a-Full ──► Phase 2b ──► Phase 2c ──► Phase 2d
  (OpenAI 代理)   (多 provider LB)  (Virtual Key) (语义缓存) (Prompt Guard)

                                    Phase 3 ──► Phase 4
                                    (MCP GW)   (Agent GW)
```

**轨道依赖说明：**
- 轨道 A 和 B **无技术依赖**，可独立推进
- Phase 3（MCP GW）依赖 Phase 2a-Full 的通用限流器基础设施
- Phase 4（Agent GW）依赖 Phase 3 的 MCP 路由能力（Agent 调用 Tool 时走 MCP 网关）
- Phase 0 的 busted spec 框架仅用于验证传统网关，不影响 AI 网关开发

### 通用限流器基础设施设计

位于 `kong-ai` crate 内的可复用限流器，三个子网关共享：

```rust
// 通用限流器 trait — LLM/MCP/Agent 子网关通过此接口接入
trait RateLimiter: Send + Sync {
    async fn check(&self, key: &RateLimitKey, cost: u64) -> Result<RateLimitResult>;
}

struct RateLimitKey {
    dimension: RateLimitDimension,  // Consumer / Route / ApiKey / Agent / Tool
    window: Duration,               // 时间窗口
    metric: RateLimitMetric,        // Tokens / Requests / ToolCalls
}

struct RateLimitResult {
    allowed: bool,        // 是否允许本次请求
    remaining: u64,       // 当前窗口剩余配额
    reset_at: Instant,    // 窗口重置时间
    limit: u64,           // 窗口总配额
}
```

**复用路径：**
- LLM Gateway: `metric=Tokens, dimension=ApiKey` → TPM/RPM 限流
- MCP Gateway: `metric=ToolCalls, dimension=Tool` → per-tool 调用频率限制
- Agent Gateway: `metric=Requests, dimension=Agent` → per-agent RPM

**存储后端：**
- Phase 2a-MVP：本地内存固定窗口（AtomicU64 + TTL）
- Phase 2b：近似滑动窗口（双桶法），可选 Redis（`INCRBY key cost EX window`）

### Phase 2 拆分明细

| 子阶段 | 内容 | 交付物 |
|--------|------|--------|
| 2a-MVP | OpenAI 协议代理 + Token 计数 + 单 provider | 最小可演示的 LLM Gateway |
| 2a-Full | 多 provider 负载均衡 + Fallback + Anthropic/Gemini | 生产可用的多模型路由 |
| 2b | Virtual API Key（映射虚拟key→真实provider key，设置预算/限额） + Token 成本追踪（per key/team 费用统计） | 企业级 key 管理 |
| 2c | 语义缓存（向量相似度缓存 LLM 响应，优先使用 usearch 内嵌向量索引，可选 pgvector）。Embedding 来源：优先调用外部 Embedding API（OpenAI/Cohere），后期支持本地 ONNX 模型 | 性能优化 |
| 2d | Prompt Guard（正则+语义级 prompt injection 检测和内容过滤） | AI 安全 |

### Agent Gateway 能力清单（补全）

**对标产品：** Google A2A Protocol, LangGraph Platform, CrewAI, solo.io (A2A+MCP)

**核心能力：**
- Agent 注册与发现 — 通过 Admin API 注册 Agent，发布 Agent Card（类似 A2A）
- A2A 协议代理 — 代理 Agent 间的 A2A 通信，支持 SSE 流式任务
- Agent 身份与访问控制 — per-agent 认证（API Key / OAuth），per-agent 授权（哪些 Agent 可以互调）
- 会话状态路由 — 基于 session ID 的 consistent-hash 路由，将有状态对话路由到正确的 Agent 实例（复用 kong-proxy 已有的 consistent-hashing 负载均衡器）
- Agent 可观测性 — 调用链路追踪、延迟/错误率/token 使用指标

**与 MCP Gateway 的边界：**
- MCP Gateway 管理**工具**（tool）— 无状态的能力单元
- Agent Gateway 管理**智能体**（agent）— 有状态的、可以主动发起调用的实体
- Agent 调用 Tool 时，流量从 Agent Gateway 路由到 MCP Gateway

### MCP / Skill Gateway 能力清单

**对标产品：** IBM ContextForge, Microsoft MCP Gateway, Kong Enterprise, Higress

**核心能力：**
- MCP Server 注册与发现 — Admin API 注册 MCP Server 元数据（工具列表、schema、版本），Agent 通过网关 `/mcp/tools` 发现可用工具
- MCP 路由与负载均衡 — 相同能力的多个 MCP Server 做负载均衡和故障转移（复用 kong-proxy 的 upstream + balancer）
- MCP 认证/授权 — per-tool/per-agent 的访问控制，支持 API Key 和 OAuth
- MCP 可观测性 — 工具调用延迟、错误率、使用量 Prometheus 指标
- Skill 编排 — Skill 注册、组合、执行（**Phase 4 范围，本文档不展开**）

### Crate 架构图

```
kong-server (binary entry point)
 ├── kong-core          — 核心数据模型、trait、通用类型
 ├── kong-config        — 配置解析（kong.conf）
 ├── kong-db            — PostgreSQL DAO + 缓存 + db-less
 ├── kong-router        — 路由引擎（traditional + expressions + L4 stream）
 ├── kong-proxy         — Pingora 代理引擎（L7 HTTP + L4 Stream）
 ├── kong-plugin-system — 插件注册/执行框架
 ├── kong-lua-bridge    — Lua 兼容层 + PDK + ngx.*
 ├── kong-admin         — Admin API（axum）
 ├── kong-cluster       — CP/DP 集群通信
 ├── kong-ai            — LLM Gateway 核心（通用限流器、OpenAI/Anthropic 协议、Token 计数）[NEW]
 ├── kong-mcp           — MCP/Skill Gateway（MCP 协议处理、工具注册/路由）[NEW]
 └── kong-agent         — Agent Gateway（A2A 协议、Agent 注册/路由）[NEW]

依赖关系：
  kong-ai    → kong-core, kong-proxy, kong-admin
  kong-mcp   → kong-core, kong-proxy, kong-admin, kong-ai (限流器)
  kong-agent → kong-core, kong-proxy, kong-admin, kong-ai (限流器), kong-mcp (工具调用)
```

### 技术风险

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| MCP Rust SDK 生态不成熟 | Phase 3 交付延迟 | 直接实现 MCP 协议（JSON-RPC over HTTP/SSE），不依赖第三方 SDK |
| Pingora SSE 长连接支持 | Agent/LLM Gateway 核心依赖 | 已验证：Pingora 支持 SSE 流式响应（kong-proxy 已有 SSE 透传能力） |
| 语义缓存向量检索性能 | Phase 2c 延迟敏感 | 优先使用 usearch（Rust binding，内嵌式），避免外部向量数据库依赖 |
| A2A 协议尚在演进 | Phase 4 接口不稳定 | Phase 4 最晚交付，协议稳定后再实现；先实现自定义 Agent 路由协议 |
| Token 计数准确性 | LLM 计费依赖精确 token 数 | Phase 2a-MVP 用 `tiktoken-rs` 支持 GPT 系列，其他 provider 用 char/4 近似；Phase 2a-Full 按 provider 接入准确 tokenizer |
| 语义缓存 embedding 来源 | 需要额外网络调用和成本 | 优先调用外部 Embedding API（如 OpenAI text-embedding-3-small），后期支持本地 ONNX 模型减少依赖 |
| usearch Rust binding 成熟度 | 向量索引 API 稳定性 | 备选方案：hora（纯 Rust HNSW）或退化为精确线性扫描（小规模场景足够） |

### 竞品对比（更新版）

| 能力 | Kong | LiteLLM | Portkey | Helicone | Cloudflare AI GW | Higress | solo.io | **Kong-Rust** |
|------|------|---------|---------|----------|-------------------|---------|---------|-------------|
| API 网关 | ✓ | ✗ | ✗ | ✗ | ✗ | ✓ | ✗ | **✓ (Kong 兼容)** |
| LLM 代理 | Lua | ✓ 100+ | ✓ 200+ | ✓ | ✓ | ✓ | ✗ | **Rust 原生** |
| Agent 网关 | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | ✓ A2A | **Rust 原生** |
| MCP 网关 | Ent | ✓ | ✗ | ✗ | ✗ | ✓ | ✓ | **Rust 原生** |
| 可观测性 | 中 | 基础 | ✓ 强 | ✓ 核心 | ✓ | 中 | 中 | **Rust 原生** |
| 语言 | Lua | Python | TS | TS | Go | C++ | Rust | **Rust** |

**竞品表说明：** ✓=已有, ✗=无, Ent=仅企业版, Lua/TS/Python=通过该语言插件实现（非原生）

**Kong-Rust 独特定位：市场上唯一同时覆盖 API 网关 + LLM 网关 + Agent 网关 + MCP 网关的 Rust 原生项目。**

# Phase 2: AI Gateway — LLM Proxy 完整设计

Generated by brainstorming on 2026-03-22
Branch: dawxy/check-pending-tasks

---

## 1. 设计决策总结

| 决策 | 选择 | 理由 |
|------|------|------|
| 架构模式 | 插件模式（复用 PluginHandler trait） | 复用现有路由/插件链/Admin API 体系 |
| 插件组织 | 多插件组合（Higress 风格） | AI 能力自由组合，独立测试 |
| AI 配置模型 | 独立实体 + 插件引用（LiteLLM 风格） | 多 Route 共享 provider，Admin API 独立管理 |
| Token 限流后端 | 内存 + 可选 Redis | 单节点高性能，分布式可扩展 |
| 内部规范化格式 | OpenAI 格式 | 业界事实标准，Kong 原版同样选择 |
| AI 能力实现 | 全部 Rust 原生 | 项目强制约束 |
| 框架偏好 | 优先 Pingora 生态 | 项目强制约束 |

### 范围

覆盖路线图 Phase 2a ~ 2d 全量：

- **2a**: 多 Provider 代理 + 双协议暴露 + LB/Fallback + Token 计数
- **2b**: Virtual API Key + Token 成本追踪
- **2c**: 语义缓存
- **2d**: Prompt Guard

### Provider 矩阵

| Provider | 协议 | driver 类型 | MVP |
|----------|------|------------|-----|
| OpenAI | OpenAI native | `openai` | ✅ |
| Anthropic/Claude | Claude native | `anthropic` | ✅ |
| Google Gemini | Gemini native | `gemini` | ✅ |
| 通义千问 (Qwen) | OpenAI compatible | `openai_compat` | ✅ |
| 混元 (Hunyuan) | OpenAI compatible | `openai_compat` | ✅ |

### 客户端协议暴露

| 协议 | 端点 | 说明 |
|------|------|------|
| OpenAI | `POST /v1/chat/completions` | 标准 OpenAI Chat API |
| OpenAI | `POST /v1/completions` | Legacy Completions API |
| Claude | `POST /v1/messages` | Anthropic Messages API |

网关内部统一使用 OpenAI 格式作为规范化表示。Claude 协议请求在 access 阶段转换为 OpenAI 格式，响应在 header_filter/body_filter 阶段转换回 Claude 格式。

---

## 2. Crate 结构

### 2.1 依赖关系

```
kong-ai (新建)
  ├── 依赖: kong-core (trait, models, error)
  ├── 依赖: kong-db (DAO trait, PostgreSQL)
  ├── 依赖: kong-plugin-system (PluginHandler 注册)
  └── 依赖: kong-config (读取 AI 相关配置)

kong-admin (已有，新增 AI 路由)
  └── 新增依赖: kong-ai

kong-server (已有，启动时注册 AI 插件)
  └── 新增依赖: kong-ai
```

### 2.2 模块布局

```
crates/kong-ai/src/
├── lib.rs                  # crate 入口
├── models.rs               # AI 实体: AiProvider, AiModel, AiVirtualKey
├── dao.rs                  # AI 实体 CRUD (PostgreSQL)
│
├── provider/
│   ├── mod.rs              # Provider trait + ProviderRegistry + LB/Fallback
│   ├── openai.rs           # OpenAI driver
│   ├── anthropic.rs        # Anthropic/Claude driver
│   ├── gemini.rs           # Google Gemini driver
│   └── openai_compat.rs    # OpenAI 兼容 (Qwen, 混元, DeepSeek 等)
│
├── codec/
│   ├── mod.rs              # 编解码入口
│   ├── openai_format.rs    # OpenAI 请求/响应格式定义 + 序列化
│   ├── anthropic_format.rs # Claude Messages 格式定义 + 双向转换
│   └── sse.rs              # SSE 帧解析器 (text/event-stream + provider 变体)
│
├── token/
│   ├── counter.rs          # Token 计数: provider 真实值 > tiktoken > 字符估算
│   └── cost.rs             # 成本计算: per-model 价格表 + 汇总
│
├── ratelimit/
│   ├── mod.rs              # RateLimiter trait
│   ├── memory.rs           # 内存固定窗口 (dashmap + 原子计数)
│   └── redis.rs            # Redis 固定窗口 (两阶段: 预检 + 回写)
│
├── cache/
│   ├── mod.rs              # SemanticCache trait
│   ├── redis_kv.rs         # Redis KV 精确缓存
│   └── semantic.rs         # 语义缓存 (embedding API + 向量相似度)
│
├── guard/
│   ├── mod.rs              # PromptGuard trait
│   ├── regex.rs            # 正则规则匹配
│   └── semantic.rs         # 语义级检测 (外部 embedding API)
│
└── plugins/
    ├── ai_proxy.rs         # ai-proxy 插件 (核心代理 + 协议转换 + LB)
    ├── ai_rate_limit.rs    # ai-rate-limit 插件 (TPM/RPM)
    ├── ai_cache.rs         # ai-cache 插件 (语义缓存)
    └── ai_prompt_guard.rs  # ai-prompt-guard 插件 (安全检查)
```

---

## 3. 数据库实体

### 3.1 `ai_providers` — LLM Provider 配置

```sql
CREATE TABLE IF NOT EXISTS ai_providers (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name          TEXT UNIQUE NOT NULL,
    provider_type TEXT NOT NULL,                -- openai | anthropic | gemini | openai_compat
    endpoint_url  TEXT,                         -- 自定义 endpoint (NULL = 使用默认)
    auth_config   JSONB NOT NULL DEFAULT '{}',  -- { header_name, header_value, ... }
    default_model TEXT,                         -- 默认模型名
    config        JSONB NOT NULL DEFAULT '{}',  -- provider 特定配置
    enabled       BOOLEAN NOT NULL DEFAULT true,
    ws_id         UUID REFERENCES workspaces(id),
    created_at    TIMESTAMPTZ DEFAULT now(),
    updated_at    TIMESTAMPTZ DEFAULT now()
);
```

`auth_config` 结构：
```json
{
  "header_name": "Authorization",
  "header_value": "Bearer sk-xxx",
  "param_name": null,
  "param_value": null,
  "aws_access_key_id": null,
  "aws_secret_access_key": null,
  "aws_region": null,
  "gcp_service_account_json": null
}
```

`config` 结构（provider 特定）：
```json
{
  "azure_instance": "my-instance",
  "azure_api_version": "2024-02-01",
  "azure_deployment_id": "gpt-4",
  "anthropic_version": "2023-06-01",
  "gemini_project_id": "my-project",
  "gemini_location": "us-central1",
  "compat_vendor": "qwen"
}
```

### 3.2 `ai_models` — Model Group（LB 单元）

```sql
CREATE TABLE IF NOT EXISTS ai_models (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name          TEXT NOT NULL,                -- 对客户端暴露的名称 (如 "gpt-4")
    provider_id   UUID NOT NULL REFERENCES ai_providers(id) ON DELETE CASCADE,
    model_name    TEXT NOT NULL,                -- provider 侧真实模型名
    priority      INTEGER NOT NULL DEFAULT 0,   -- Fallback 优先级 (高 = 优先)
    weight        INTEGER NOT NULL DEFAULT 100, -- LB 权重
    input_cost    NUMERIC,                      -- 每百万 token 输入成本 (USD)
    output_cost   NUMERIC,                      -- 每百万 token 输出成本 (USD)
    max_tokens    INTEGER,                      -- 模型最大 token 数
    config        JSONB NOT NULL DEFAULT '{}',  -- 模型级覆盖 (temperature, top_p 等)
    enabled       BOOLEAN NOT NULL DEFAULT true,
    ws_id         UUID REFERENCES workspaces(id),
    created_at    TIMESTAMPTZ DEFAULT now(),
    updated_at    TIMESTAMPTZ DEFAULT now(),
    UNIQUE(name, provider_id, ws_id)
);
```

**Model Group 机制**：`ai_models.name` 相同的多条记录组成一个 model group。ai-proxy 插件按 `name` 查找 group，在 group 内做 LB + Fallback。

### 3.3 `ai_virtual_keys` — Virtual API Key

```sql
CREATE TABLE IF NOT EXISTS ai_virtual_keys (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name          TEXT UNIQUE NOT NULL,
    key_hash      TEXT UNIQUE NOT NULL,         -- SHA-256(raw_key)
    key_prefix    TEXT NOT NULL,                -- 前 8 字符 (日志/识别用)
    consumer_id   UUID REFERENCES consumers(id) ON DELETE SET NULL,
    allowed_models TEXT[] DEFAULT '{}',         -- 允许的 model name (空 = 全部)
    tpm_limit     INTEGER,                      -- Token Per Minute
    rpm_limit     INTEGER,                      -- Request Per Minute
    budget_limit  NUMERIC,                      -- 总预算上限 (USD)
    budget_used   NUMERIC NOT NULL DEFAULT 0,   -- 已使用预算
    enabled       BOOLEAN NOT NULL DEFAULT true,
    expires_at    TIMESTAMPTZ,
    ws_id         UUID REFERENCES workspaces(id),
    created_at    TIMESTAMPTZ DEFAULT now(),
    updated_at    TIMESTAMPTZ DEFAULT now()
);
```

### 3.4 实体关系

AI 插件支持 Kong 标准的四级挂载：**全局 / Per-Service / Per-Route / Per-Consumer**。
插件通过现有 `resolve_plugins` 机制自动处理四级优先级覆盖（global < service < route < consumer）。

```
┌─────────────────── 挂载方式（任选）───────────────────┐
│                                                      │
│  全局:    POST /plugins { name: "ai-proxy", ... }    │
│  Service: POST /services/{id}/plugins                │
│  Route:   POST /routes/{id}/plugins                  │
│  Consumer: POST /consumers/{id}/plugins              │
│                                                      │
└──────────────────────┬───────────────────────────────┘
                       │
                       ▼
              ai-proxy 插件 config
              { model: "gpt-4",  model_source: "config"|"request" }
                       │
                       ▼ (按 name 查找 model group)
              ai_models (name="gpt-4")
              ├── model_1: provider=my-openai,  model_name=gpt-4,     weight=80, priority=10
              ├── model_2: provider=my-azure,   model_name=gpt-4-0613, weight=20, priority=10
              └── model_3: provider=my-anthropic, model_name=claude-3.5, weight=100, priority=5 (fallback)
                       │
                       ▼
              ai_providers (endpoint + auth)

ai_virtual_keys ──关联──> consumers (复用 Kong Consumer 体系)
```

**典型配置示例：**

| 场景 | 挂载方式 | 说明 |
|------|---------|------|
| 所有 AI 请求统一安全检查 | 全局挂载 `ai-prompt-guard` | 不需要每个 Route 单独配置 |
| 某 Service 下所有 Route 共享同一 LLM | Service 级挂载 `ai-proxy` | config.model="gpt-4" |
| 单个 Route 指定特定模型 | Route 级挂载 `ai-proxy` | 覆盖 Service 级配置 |
| 统一 LLM 入口，model 由请求决定 | 全局/Service 级挂载 `ai-proxy` | config.model_source="request" |
| 特定 Consumer 限流 | Consumer 级挂载 `ai-rate-limit` | 覆盖全局限流配置 |

---

## 4. Provider Trait 与 Driver 设计

### 4.1 核心 Trait

```rust
/// AI Provider 数据库实体对应的配置 struct（非 trait）
pub struct AiProviderConfig {
    pub id: Uuid,
    pub name: String,
    pub provider_type: String,
    pub endpoint_url: Option<String>,
    pub auth_config: AuthConfig,
    pub default_model: Option<String>,
    pub config: serde_json::Value,
    pub enabled: bool,
}

/// LLM Provider 驱动接口
/// 每个 provider (OpenAI, Anthropic, Gemini...) 实现此 trait
#[async_trait]
pub trait AiDriver: Send + Sync {
    /// driver 类型标识
    fn provider_type(&self) -> &str;

    /// 将 OpenAI 规范化请求转换为 provider 原生格式
    fn transform_request(
        &self,
        request: &ChatRequest,
        model: &AiModel,
        provider_config: &AiProviderConfig,
    ) -> Result<ProviderRequest>;

    /// 将 provider 原生响应转换为 OpenAI 规范化格式 (非流式)
    fn transform_response(
        &self,
        status: u16,
        headers: &HeaderMap,
        body: &Bytes,
        model: &AiModel,
    ) -> Result<ChatResponse>;

    /// 将 provider SSE event 转换为 OpenAI 规范化 SSE (流式)
    fn transform_stream_event(
        &self,
        event: &SseEvent,
        model: &AiModel,
    ) -> Result<Option<SseEvent>>;

    /// 配置上游连接参数 (URL, headers, auth)
    fn configure_upstream(
        &self,
        model: &AiModel,
        provider_config: &AiProviderConfig,
    ) -> Result<UpstreamConfig>;

    /// 从 provider 响应中提取 token 使用量
    fn extract_usage(&self, body: &Bytes) -> Option<TokenUsage>;

    /// 从流式 event 中提取增量 token 使用量
    fn extract_stream_usage(&self, event: &SseEvent) -> Option<TokenUsage>;
}

pub struct ProviderRequest {
    pub body: Bytes,
    pub content_type: String,
    pub extra_headers: Vec<(String, String)>,
}

pub struct UpstreamConfig {
    pub scheme: String,         // "https"
    pub host: String,           // "api.openai.com"
    pub port: u16,              // 443
    pub path: String,           // "/v1/chat/completions"
    pub headers: Vec<(String, String)>, // auth headers 等
}

pub struct TokenUsage {
    pub prompt_tokens: Option<u64>,
    pub completion_tokens: Option<u64>,
    pub total_tokens: Option<u64>,
}
```

### 4.2 Driver 注册

```rust
pub struct DriverRegistry {
    drivers: HashMap<String, Arc<dyn AiDriver>>,
}

impl DriverRegistry {
    pub fn new() -> Self {
        let mut registry = Self { drivers: HashMap::new() };
        registry.register("openai", Arc::new(OpenAiDriver));
        registry.register("anthropic", Arc::new(AnthropicDriver));
        registry.register("gemini", Arc::new(GeminiDriver));
        registry.register("openai_compat", Arc::new(OpenAiCompatDriver));
        registry
    }

    pub fn get(&self, provider_type: &str) -> Option<&Arc<dyn AiDriver>> {
        self.drivers.get(provider_type)
    }
}
```

### 4.3 Driver 实现要点

**OpenAI driver**：最简单 — 内部格式即 OpenAI 格式，transform_request/response 几乎透传。设置 `Authorization: Bearer <key>` header。

**Anthropic driver**：
- 请求：OpenAI messages → Anthropic messages（system 提取为独立字段，tool_calls 格式转换）
- 响应：Anthropic content blocks → OpenAI choices
- 流式：处理 `message_start`/`content_block_delta`/`message_delta`/`message_stop` 事件类型
- Header：`x-api-key` + `anthropic-version`

**Gemini driver**：
- 请求：OpenAI messages → Gemini `generateContent` format（`contents` 数组，role 映射 user/model）
- 响应：Gemini candidates → OpenAI choices
- 流式：使用 `streamGenerateContent?alt=sse` 端点获取标准 SSE 格式（避免 JSON array 状态机解析的复杂度）
- Auth：API key 作为 query param 或 OAuth2 bearer token

**OpenAI Compat driver**：
- 继承 OpenAI driver 的全部转换逻辑
- 允许自定义 endpoint_url（如 `https://dashscope.aliyuncs.com/compatible-mode`）
- 允许自定义 auth header（如 Qwen 用 `Authorization: Bearer <dashscope-key>`）
- `config.compat_vendor` 字段标记具体厂商，用于处理细微差异

---

## 5. 协议编解码（Codec）

### 5.1 OpenAI 格式（内部规范化格式）

```rust
/// OpenAI Chat Completion 请求
#[derive(Serialize, Deserialize)]
pub struct ChatRequest {
    pub model: String,
    pub messages: Vec<Message>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub temperature: Option<f64>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub max_tokens: Option<u64>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub top_p: Option<f64>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub stream: Option<bool>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub tools: Option<Vec<Tool>>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub tool_choice: Option<ToolChoice>,
    // ... 其他可选字段 flatten
    #[serde(flatten)]
    pub extra: HashMap<String, serde_json::Value>,
}

#[derive(Serialize, Deserialize)]
pub struct Message {
    pub role: String,  // "system" | "user" | "assistant" | "tool"
    #[serde(skip_serializing_if = "Option::is_none")]
    pub content: Option<MessageContent>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub tool_calls: Option<Vec<ToolCall>>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub tool_call_id: Option<String>,
}

/// ChatResponse, ChatChoice, SseEvent 等类似定义...
```

### 5.2 Claude 协议转换（客户端侧）

网关对外暴露 Claude 协议 (`/v1/messages`)，需要双向转换：

```rust
pub struct AnthropicCodec;

impl AnthropicCodec {
    /// Claude Messages 请求 → OpenAI ChatRequest
    pub fn decode_request(body: &Bytes) -> Result<ChatRequest> {
        // 1. 解析 Anthropic Messages 格式
        // 2. system 字段 → messages[0] { role: "system" }
        // 3. content blocks → 统一 content 格式
        // 4. tool_use → tool_calls 格式映射
    }

    /// OpenAI ChatResponse → Claude Messages 响应
    pub fn encode_response(response: &ChatResponse) -> Result<Bytes> {
        // 逆向转换
    }

    /// OpenAI SSE event → Claude SSE event (流式)
    pub fn encode_stream_event(event: &SseEvent) -> Result<Vec<SseEvent>> {
        // OpenAI 单 delta → Claude 多事件 (message_start + content_block_start + delta + stop)
    }
}
```

### 5.3 SSE 解析器

```rust
/// 通用 SSE 帧解析器，支持跨 chunk 不完整帧重组
pub struct SseParser {
    buffer: BytesMut,          // 未消费的 buffer
    content_type: SseFormat,   // 检测到的流式格式
}

pub enum SseFormat {
    Standard,                  // text/event-stream (OpenAI, Anthropic, Mistral)
    GeminiJsonArray,           // application/json (Gemini 返回 JSON 数组)
    AwsEventStream,            // application/vnd.amazon.eventstream (Bedrock)
    Ndjson,                    // application/x-ndjson (Cohere, 部分自托管)
}

impl SseParser {
    /// 喂入 raw chunk，产出零或多个 SseEvent
    pub fn feed(&mut self, chunk: &Bytes) -> Vec<SseEvent>;

    /// 流结束时刷出 buffer 中的残余
    pub fn flush(&mut self) -> Vec<SseEvent>;
}

pub struct SseEvent {
    pub event_type: Option<String>,  // "event:" 字段
    pub data: String,                // "data:" 字段
    pub id: Option<String>,          // "id:" 字段
}
```

---

## 6. 插件设计

### 6.1 ai-proxy 插件（Priority: 770）

核心代理插件，负责请求/响应的协议转换、上游路由、LB/Fallback。

**Config Schema：**
```json
{
  "model": "gpt-4",                     // model group name (model_source=config 时必填)
  "model_source": "config",             // config: 使用固定 model | request: 从请求体 model 字段动态路由
  "route_type": "llm/v1/chat",          // llm/v1/chat | llm/v1/completions
  "client_protocol": "openai",          // openai | anthropic (客户端协议)
  "response_streaming": "allow",        // allow | deny | always
  "max_request_body_size": 8192,        // 最大请求体 (KB)
  "model_name_header": true,            // X-Kong-LLM-Model 响应头
  "timeout": 60000,                     // LLM 请求超时 (ms), 流式请求仅作为首包超时
  "retries": 2,                         // group 内重试次数
  "log_payloads": false,                // 是否记录请求/响应原文
  "log_statistics": true                // 是否记录 token/cost 指标
}
```

**插件间数据传递：**

现有 `RequestCtx.shared` 是 `HashMap<String, serde_json::Value>`，无法存储 trait object 和有状态对象（如 `SseParser`）。需要扩展 `RequestCtx`：

```rust
// 在 kong-core 的 RequestCtx 中新增
pub extensions: anymap2::Map<dyn Any + Send + Sync>,
```

AI 插件将类型化状态存入 extensions：

```rust
/// AI 插件跨阶段共享状态（存入 ctx.extensions）
pub struct AiRequestState {
    pub driver: Arc<dyn AiDriver>,
    pub model: AiModel,
    pub provider_config: AiProviderConfig,
    pub stream_mode: bool,
    pub client_protocol: ClientProtocol,  // OpenAI | Anthropic
    pub sse_parser: Option<SseParser>,    // 流式时使用
    pub usage: TokenUsage,                // 累加器
    pub response_buffer: Option<BytesMut>, // 流式时累积完整响应（供缓存回写）
    pub ttft: Option<Instant>,            // Time To First Token
}
```

**执行流程：**

```
access 阶段:
  1. 解析请求体 → ChatRequest (内部格式)
     - client_protocol=anthropic: AnthropicCodec.decode_request()
     - client_protocol=openai: 直接反序列化
  2. 查找 model group → 按 weight/priority LB 选择一条 AiModel
  3. 获取 AiProviderConfig → 调用 driver.transform_request() 转换为 provider 格式
  4. 调用 driver.configure_upstream() → 覆写 ctx 的 upstream_host/port/scheme/path
  5. 设置 upstream 请求 headers (auth + content-type + extra)
  6. 设置 ctx.upstream_body = Some(provider_body) (替换请求体)
  7. 对 OpenAI/Azure provider 自动注入 stream_options.include_usage=true
  8. 将 AiRequestState 存入 ctx.extensions

header_filter 阶段:
  9. 检测上游响应 Content-Type，判断流式/非流式
  10. 流式: 设置响应 Content-Type=text/event-stream，初始化 SseParser 存入 AiRequestState
  11. 非流式: 标记等待 body_filter 完整 body

body_filter 阶段:
  12. 非流式 (end_of_stream=true 时):
      - 从 buffered body 完整读取
      - driver.transform_response() → ChatResponse
      - driver.extract_usage() → TokenUsage
      - client_protocol=anthropic: AnthropicCodec.encode_response()
      - 替换响应体，设置 X-Kong-LLM-Model header
  13. 流式 (逐 chunk):
      - SseParser.feed(chunk) → events
      - 对每个 event: driver.transform_stream_event() → OpenAI 格式 event
      - driver.extract_stream_usage() → 累加到 AiRequestState.usage
      - client_protocol=anthropic: AnthropicCodec.encode_stream_event()
      - 同步累积到 response_buffer (供 ai-cache 回写)
      - 拼接输出 chunk
  14. 流式 end_of_stream: SseParser.flush()，完成最终 usage 汇总

log 阶段:
  15. 序列化 analytics: model, provider, latency, token usage, cost
  16. 异步更新 budget_used (如果有 virtual key)
  17. 上报 LB 健康状态: report_success() 或 report_failure()
```

**LB/Fallback 策略：**

```rust
pub struct ModelGroupBalancer {
    /// 同 priority 的 models 之间做加权 round-robin
    /// 不同 priority 之间做 fallback (高 priority 优先)
    models: Vec<AiModel>,
    providers: HashMap<Uuid, AiProviderConfig>,
    health: HashMap<Uuid, ModelHealth>,
}

pub struct ModelHealth {
    consecutive_failures: u32,
    last_failure_at: Option<Instant>,
    cooldown_until: Option<Instant>,  // 故障冷却期
}

impl ModelGroupBalancer {
    /// 选择一个健康的 model
    /// 1. 按 priority 降序分组
    /// 2. 在最高 priority 组内按 weight 加权 round-robin
    /// 3. 该组全部不健康 → fallback 到下一个 priority 组
    pub fn select(&self) -> Result<(&AiModel, &AiProviderConfig)>;

    /// 标记请求成功
    pub fn report_success(&self, model_id: &Uuid);

    /// 标记请求失败，连续失败超过阈值进入冷却期
    pub fn report_failure(&self, model_id: &Uuid);
}
```

冷却策略：
- 连续 3 次失败 → 进入 30s 冷却期
- 冷却期结束后自动恢复（允许探测请求）
- 429 (Rate Limit) → 立即 10s 冷却

### 6.2 ai-rate-limit 插件（Priority: 771，在 ai-proxy 之前）

**Config Schema：**
```json
{
  "limit_by": "virtual_key",      // virtual_key | consumer | route | global
  "tpm_limit": 100000,            // Token Per Minute (优先使用 virtual_key 上的值)
  "rpm_limit": 1000,              // Request Per Minute
  "storage": "memory",            // memory | redis
  "redis": {                      // storage=redis 时生效
    "host": "localhost",
    "port": 6379,
    "password": null,
    "database": 0,
    "timeout": 2000
  },
  "header_name": "X-AI-Key",     // 从哪个 header 读取 virtual key
  "error_code": 429,
  "error_message": "AI rate limit exceeded"
}
```

**两阶段执行：**

```
access 阶段 (预检):
  1. 从 header 提取 virtual key → hash → 查 ai_virtual_keys 表
  2. 验证 key 有效性 (enabled, expires_at, allowed_models)
  3. 检查 RPM 计数是否超限 → 超限返回 429
  4. 检查 TPM 计数是否超限 → 超限返回 429
  5. RPM 计数 +1
  6. 估算 prompt_tokens 并预扣 TPM (缓解回写窗口问题)
  7. 将 virtual_key 信息存入 ctx (供 ai-proxy 和 log 阶段使用)

log 阶段 (回写):
  8. 从 ctx 读取实际 token usage
  9. TPM 计数修正: += (actual_tokens - 预扣的 estimated_tokens)
  10. 异步更新 budget_used += cost
```

**内存限流器：**
```rust
pub struct MemoryRateLimiter {
    /// key → (window_start, count)
    /// 使用 dashmap 无锁并发
    windows: DashMap<String, (Instant, u64)>,
}
```

**Redis 限流器（两阶段 Lua 脚本）：**
```
-- 预检阶段
local key = KEYS[1]
local limit = tonumber(ARGV[1])
local window = tonumber(ARGV[2])
local current = redis.call('GET', key)
if current and tonumber(current) >= limit then
    return 0  -- 超限
end
return 1  -- 放行

-- 回写阶段
local key = KEYS[1]
local tokens = tonumber(ARGV[1])
local window = tonumber(ARGV[2])
local current = redis.call('INCRBY', key, tokens)
if current == tokens then
    redis.call('EXPIRE', key, window)
end
return current
```

### 6.3 ai-cache 插件（Priority: 772，在 ai-proxy 之前）

**Config Schema：**
```json
{
  "cache_ttl": 3600,              // 缓存 TTL (秒)
  "cache_key_strategy": "last_question",  // last_question | all_questions
  "storage": "redis",             // redis (KV 精确匹配)
  "redis": { "host": "...", "port": 6379 },
  "semantic_cache": {             // 可选: 语义缓存
    "enabled": false,
    "similarity_threshold": 0.85, // 相似度阈值
    "embedding_provider_id": null,// 使用哪个 ai_provider 生成 embedding
    "embedding_model": "text-embedding-3-small",
    "vector_store": "redis"       // redis (RediSearch) | pgvector
  },
  "skip_header": "X-AI-Skip-Cache"  // 请求携带此 header 跳过缓存
}
```

**执行流程：**

```
access 阶段:
  1. 检查 skip_header → 跳过
  2. 提取 cache key (last user message 或 all messages 拼接)
  3. Redis KV 精确匹配 → 命中则直接返回 (短路, 设 X-Cache-Status: HIT)
  4. 如果 semantic_cache.enabled:
     a. 调用 embedding API 获取向量
     b. 向量相似度搜索 → score > threshold → 语义命中
     c. 从 Redis 获取对应缓存响应 → 短路返回
  5. 未命中 → 放行，标记 ctx.cache_key

log 阶段 (回写):
  6. 从 ctx.extensions 的 AiRequestState.response_buffer 读取完整响应
     (ai-proxy 在 body_filter 流式处理时同步累积到 response_buffer)
  7. 写入 Redis KV (key → response, TTL)
  8. 如果 semantic_cache.enabled:
     a. 上传 embedding + cache_key 到向量存储
```

### 6.4 ai-prompt-guard 插件（Priority: 773，在 ai-proxy 之前）

**Config Schema：**
```json
{
  "deny_patterns": [              // 正则拒绝规则
    "ignore previous instructions",
    "system prompt",
    "\\bpassword\\b.*\\b(reveal|show|tell)\\b"
  ],
  "allow_patterns": [],           // 正则放行规则 (白名单模式，非空时只放行匹配的)
  "max_message_length": 32768,    // 单条消息最大字符数
  "semantic_guard": {             // 可选: 语义级检测
    "enabled": false,
    "provider_id": null,          // 用于检测的 AI provider
    "model": "gpt-4o-mini",      // 检测模型
    "categories": ["injection", "jailbreak", "pii"],
    "threshold": 0.8
  },
  "action": "block",             // block | log_only
  "error_code": 400,
  "error_message": "Request blocked by AI security policy"
}
```

**执行流程：**

```
access 阶段:
  1. 解析请求体 messages
  2. 对每条 user message:
     a. 检查长度 → 超限阻断
     b. 逐条匹配 deny_patterns → 命中阻断
     c. allow_patterns 非空时检查白名单 → 不匹配阻断
  3. 如果 semantic_guard.enabled:
     a. 调用检测模型分析 prompt
     b. 任一 category score > threshold → 阻断
  4. action=block → 返回 error_code; action=log_only → 标记日志但放行
```

### 6.5 插件优先级与执行顺序

```
Priority 773: ai-prompt-guard   (安全检查，最先执行)
Priority 772: ai-cache          (缓存命中则短路，跳过后续)
Priority 771: ai-rate-limit     (限流检查)
Priority 770: ai-proxy          (核心代理)
```

---

## 7. Token 计数策略

三级 fallback（与 Kong 原版一致）：

| 优先级 | 来源 | 准确度 | 适用场景 |
|--------|------|--------|----------|
| 1 | Provider 返回的真实 usage | 精确 | 非流式全部 provider；流式 Anthropic/Gemini；流式 OpenAI (通过 `stream_options.include_usage`) |
| 2 | tiktoken-rs 本地计算 | 精确 (GPT 系列) | Provider 未返回 usage 时的 fallback |
| 3 | 字符估算 (len/4) | 粗略 | tiktoken 不支持的模型 |

**注意**：ai-proxy 在 `transform_request` 时自动为 OpenAI/Azure provider 注入 `stream_options: { include_usage: true }`，确保流式响应的最后一个 SSE event 包含精确 usage。

```rust
pub struct TokenCounter {
    /// tiktoken-rs 编码器缓存 (按 model 名)
    encoders: HashMap<String, CoreBPE>,
}

impl TokenCounter {
    /// 精确计数 (如果有 tiktoken 支持)
    pub fn count_exact(&self, model: &str, text: &str) -> Option<u64>;

    /// 估算 (字符数 / 4)
    pub fn count_estimate(text: &str) -> u64;

    /// 三级 fallback
    pub fn count(&self, model: &str, text: &str, provider_usage: Option<u64>) -> u64 {
        provider_usage
            .or_else(|| self.count_exact(model, text))
            .unwrap_or_else(|| Self::count_estimate(text))
    }
}
```

---

## 8. Admin API 端点

### 8.1 AI Provider CRUD

```
GET    /ai-providers                    # 列表 (分页)
POST   /ai-providers                    # 创建
GET    /ai-providers/{id_or_name}       # 查询
PATCH  /ai-providers/{id_or_name}       # 更新 (深度合并)
PUT    /ai-providers/{id_or_name}       # 替换
DELETE /ai-providers/{id_or_name}       # 删除

GET    /ai-providers/{id}/ai-models     # 该 provider 下的 models

GET    /ai-model-groups                    # 列出所有 distinct model name (防拼写错误)
```

### 8.2 AI Model CRUD

```
GET    /ai-models                       # 列表 (分页, 支持 ?name=gpt-4 过滤)
POST   /ai-models                       # 创建
GET    /ai-models/{id}                  # 查询
PATCH  /ai-models/{id}                  # 更新
PUT    /ai-models/{id}                  # 替换
DELETE /ai-models/{id}                  # 删除
```

### 8.3 AI Virtual Key CRUD

```
GET    /ai-virtual-keys                 # 列表 (分页)
POST   /ai-virtual-keys                 # 创建 (返回一次性明文 key)
GET    /ai-virtual-keys/{id_or_name}    # 查询 (不返回 key_hash)
PATCH  /ai-virtual-keys/{id_or_name}    # 更新
DELETE /ai-virtual-keys/{id_or_name}    # 删除

POST   /ai-virtual-keys/{id}/rotate    # 轮换 key (生成新 key, 旧 key 立即失效)
GET    /ai-virtual-keys/{id}/usage     # 查询用量统计
```

### 8.4 AI Analytics

```
GET    /ai/analytics/summary           # 总体使用统计 (total tokens, cost, requests)
GET    /ai/analytics/by-model          # 按 model 分组统计
GET    /ai/analytics/by-consumer       # 按 consumer 分组统计
```

---

## 9. 可观测性

### 9.1 日志字段

ai-proxy 在 log 阶段向 Kong access log 注入以下字段：

```json
{
  "ai": {
    "proxy": {
      "provider": "openai",
      "model": "gpt-4",
      "route_type": "llm/v1/chat",
      "stream": true
    },
    "usage": {
      "prompt_tokens": 150,
      "completion_tokens": 80,
      "total_tokens": 230,
      "cost": 0.00069
    },
    "latency": {
      "e2e_ms": 1250,
      "ttft_ms": 320,
      "tpot_ms": 11.4
    },
    "cache": {
      "status": "miss"
    },
    "virtual_key": {
      "id": "xxx",
      "name": "dev-team-key"
    }
  }
}
```

### 9.2 Prometheus 指标

```
# ai-proxy
kong_ai_requests_total{model, provider, status, consumer}
kong_ai_prompt_tokens_total{model, provider}
kong_ai_completion_tokens_total{model, provider}
kong_ai_usage_cost_total{model, provider}
kong_ai_request_latency_ms{model, provider, quantile}
kong_ai_ttft_ms{model, provider, quantile}

# ai-rate-limit
kong_ai_rate_limit_exceeded_total{model, limit_by, limit_type}

# ai-cache
kong_ai_cache_hit_total{model, cache_type}   # cache_type: exact | semantic
kong_ai_cache_miss_total{model}

# ai-prompt-guard
kong_ai_prompt_blocked_total{model, reason}
```

---

## 10. 请求完整生命周期

```
客户端请求 (OpenAI 或 Claude 协议)
    │
    ▼ Route 匹配
    │
    ▼ ai-prompt-guard.access()  ← 安全检查，拒绝危险 prompt
    │
    ▼ ai-cache.access()         ← 缓存查找，命中则短路返回
    │
    ▼ ai-rate-limit.access()    ← Virtual Key 认证 + TPM/RPM 预检
    │
    ▼ ai-proxy.access()         ← 解析请求 → model group LB → provider 转换 → 设置上游
    │
    ▼ Pingora upstream_peer()   ← 连接 LLM provider (由 ai-proxy 覆写的 host:port)
    │
    ▼ ai-proxy.header_filter()  ← 非流式: 转换响应; 流式: 初始化 SSE parser
    │
    ▼ ai-proxy.body_filter()    ← 流式: 逐 chunk 转换 SSE events
    │
    ▼ ai-proxy.log()            ← 序列化 analytics, 异步更新 budget
    ▼ ai-rate-limit.log()       ← TPM 回写
    ▼ ai-cache.log()            ← 缓存回写
    │
    ▼ 响应返回客户端
```

---

## 11. 关键依赖

| 依赖 | 用途 | 说明 |
|------|------|------|
| `serde` / `serde_json` | JSON 序列化 | 已在项目中使用 |
| `tiktoken-rs` | GPT 系列精确 token 计数 | Rust 原生 BPE 实现 |
| `sha2` | Virtual Key SHA-256 哈希 | 轻量 |
| `dashmap` | 内存限流器并发 HashMap | 无锁 |
| `redis` (crate) | Redis 连接 (限流 + 缓存) | async, 可选依赖 |
| `regex` | Prompt Guard 正则匹配 | 已在项目中使用 |
| `bytes` / `bytes-mut` | SSE 解析 buffer | 已在项目中使用 |
| `anymap2` | ctx.extensions 类型化存储 | 类似 http::Extensions |

**不引入**：
- 无额外 HTTP client — 复用 Pingora 的上游连接机制
- 无向量数据库 client（MVP 用 Redis + RediSearch，pgvector 通过 sqlx 查询）

---

## 12. 已知限制

| 限制 | 原因 | 缓解方案 |
|------|------|---------|
| TPM 限流存在短暂超额窗口 | 两阶段回写固有限制：access 预检 → log 回写之间有时间差，流式请求可能持续数十秒 | access 阶段按 prompt_tokens 估算预扣；completion_tokens 在 log 阶段差额修正 |
| `ai_providers.auth_config` 明文存储 API Key | MVP 阶段简化实现 | Admin API GET 响应中 mask 敏感字段（`header_value` → `"***"`）；后续版本支持 vault 集成 |
| `ModelHealth` 健康状态纯内存 | 重启后丢失，冷却中的 provider 立即恢复 | 预期行为——重启意味着新的探测周期，无需持久化 |
| Budget 异步更新有超支窗口 | 与 LiteLLM 一致，log 阶段异步累加 | 可接受——budget 是软限制，精确到分钟级即可 |
| 流式响应缓存需要内存累积 | ai-proxy 在 body_filter 流式处理时同步累积完整响应到 `AiRequestState.response_buffer` | 大响应可能占用较多内存，可通过 `max_cache_body_size` 配置上限 |
| Redis 不可用时限流降级 | 配置 `storage=redis` 但 Redis 宕机时，需要 fallback 而非拒绝所有请求 | 自动 fallback 到内存限流器，log 告警 |

### AI 内部缓存机制

ai-proxy 插件内部使用 `Arc<RwLock<HashMap>>` 缓存 model groups、providers 和预构建的 `ModelGroupBalancer`。
AI 实体（ai_providers / ai_models）的 CUD 操作通过现有 debounce refresh channel 触发缓存重建。
请求热路径只做 `Arc::clone` 获取 balancer 引用，无锁竞争。

---

## 13. 不在范围内

- 多 CP 间 AI 配置同步（依赖 Phase 1 Hybrid）
- AI 插件的 Lua 兼容层（ai-proxy Lua 插件已有，保持共存）
- WebAssembly 插件支持
- 图片/音频/视频等多模态支持（后续版本）
- Embedding API 代理（仅内部使用，不对外暴露）
- 企业版 Kong Manager AI 面板（Phase 5）
